<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <!-- OBLIGATOIRE : PModuleIF.dll (Fuji DE100) et cspstat.dll (DNP) sont en 32 bits.
         Un processus 64 bits lève BadImageFormatException au premier appel. -->
    <PlatformTarget>x86</PlatformTarget>
    <AssemblyName>DeviceProbe</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Studio.Printing\Studio.Printing.csproj" />
  </ItemGroup>

</Project>
