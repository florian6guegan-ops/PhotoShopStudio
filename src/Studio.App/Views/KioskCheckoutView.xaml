<UserControl x:Class="Studio.App.Views.KioskCheckoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PageTitle}" Text="Quel format voulez-vous ?"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProductsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="300" MinHeight="140" Margin="10" CornerRadius="14"
                                Background="{StaticResource CardBrush}"
                                BorderThickness="5" BorderBrush="{Binding BorderBrush}"
                                MouseLeftButtonUp="OnProductClicked" Tag="{Binding}">
                            <StackPanel VerticalAlignment="Center" Margin="16">
                                <TextBlock Text="{Binding Name}" FontSize="26" FontWeight="Bold"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           Foreground="{StaticResource TextBrush}"/>
                                <TextBlock Text="{Binding PriceLabel}" FontSize="22"
                                           TextAlignment="Center" Margin="0,8,0,0"
                                           Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="16" Margin="0,16,0,0">
            <DockPanel>
                <TextBlock x:Name="SummaryText" DockPanel.Dock="Left" FontSize="24" FontWeight="Bold"
                           VerticalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
                <Button x:Name="SendButton" DockPanel.Dock="Right" Style="{StaticResource BigButton}"
                        HorizontalAlignment="Right" IsEnabled="False" Click="OnSend">
                    ✓  Envoyer ma commande
                </Button>
            </DockPanel>
        </Border>

        <!-- voile d'envoi -->
        <Border x:Name="BusyPanel" Grid.RowSpan="3" Background="#B3000000" Visibility="Collapsed">
            <TextBlock x:Name="BusyText" Foreground="White" FontSize="30" FontWeight="SemiBold"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Text="Envoi de votre commande…"/>
        </Border>
    </Grid>
</UserControl>
