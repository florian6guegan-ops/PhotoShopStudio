<UserControl x:Class="Studio.App.Views.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PageTitle}" Text="Catalogue et imprimantes"/>
        <TextBlock Grid.Row="1" Style="{StaticResource Hint}" Margin="0,0,0,16"
                   Text="« Capturer réglages » ouvre le dialogue du pilote : choisissez papier, format et finition, puis validez — ces réglages seront rejoués à chaque impression du produit. À faire imprimante branchée et allumée. L'édition des produits se fait pour l'instant dans catalog\products.json."/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProductsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource CardBrush}" CornerRadius="12"
                                Padding="16" Margin="0,0,0,10">
                            <DockPanel>
                                <Button DockPanel.Dock="Right" Style="{StaticResource FlatButton}"
                                        MinHeight="48" MinWidth="220" FontSize="18" Padding="16,6"
                                        VerticalAlignment="Center" Click="OnCaptureDevmode" Tag="{Binding}">
                                    ⚙  Capturer réglages
                                </Button>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock FontSize="22" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextBrush}" Text="{Binding Title}"/>
                                    <TextBlock FontSize="17" Foreground="{StaticResource MutedBrush}"
                                               Text="{Binding Details}" Margin="0,4,0,0"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
