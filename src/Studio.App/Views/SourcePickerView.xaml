<UserControl x:Class="Studio.App.Views.SourcePickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PageTitle}"
                   Text="Où sont les photos ?"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Style="{StaticResource Hint}" Margin="0,0,0,12"
                           Text="Insérez une clé USB, une carte mémoire ou un disque : il apparaîtra ici automatiquement."/>
                <ItemsControl x:Name="DrivesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource BigButton}" Margin="0,8"
                                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                    Click="OnDriveClicked" Tag="{Binding RootPath}">
                                <TextBlock Text="{Binding Label}"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Dossiers de l'ordinateur mis en avant : pour l'E-Photo, la photo
                     arrive par courriel ou par téléphone et atterrit dans Téléchargements,
                     pas sur une carte mémoire. -->
                <ItemsControl x:Name="RaccourcisList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource BigButton}" Margin="0,8"
                                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                    Click="OnRaccourciClicked" Tag="{Binding Chemin}"
                                    ToolTip="{Binding Chemin}">
                                <TextBlock Text="{Binding Libelle}"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock x:Name="NoDrivesText" Style="{StaticResource Hint}" Margin="0,16"
                           Text="Aucun support détecté pour l'instant…"/>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" Style="{StaticResource FlatButton}" HorizontalAlignment="Left"
                Margin="0,16,0,0" Click="OnBrowseFolder"
                ToolTip="Descendre dans les dossiers et voir ce qu'ils contiennent avant d'ouvrir">
            📁  Parcourir les dossiers de l'ordinateur…
        </Button>
    </Grid>
</UserControl>
