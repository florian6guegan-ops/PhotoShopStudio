<UserControl x:Class="Studio.App.Views.KioskDoneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource PageBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="✅" FontSize="80" TextAlignment="Center"/>
            <TextBlock Text="Commande envoyée !" FontSize="44" FontWeight="Bold"
                       TextAlignment="Center" Margin="0,12,0,24"
                       Foreground="{DynamicResource TextBrush}"/>
            <TextBlock Text="Votre numéro :" FontSize="28" TextAlignment="Center"
                       Foreground="{DynamicResource MutedBrush}"/>
            <TextBlock x:Name="NumberText" FontSize="110" FontWeight="Bold" TextAlignment="Center"
                       Foreground="{DynamicResource AccentBrush}"/>
            <TextBlock Text="Présentez-vous à la caisse, vos photos arrivent." FontSize="26"
                       TextAlignment="Center" Margin="0,20,0,40"
                       Foreground="{DynamicResource TextBrush}"/>
            <Button Style="{StaticResource BigButton}" MinWidth="380" Click="OnFinish">
                Terminer
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
